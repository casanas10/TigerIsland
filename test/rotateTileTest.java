import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;

import static org.junit.Assert.*;

/**
 * Created by Eric on 3/22/2017.
 */

/*
public class rotateTileTest {
    rotateTile test;
    @Before
    public void setUp() throws Exception {
        test = new rotateTile(201, 0);
    }

    @Test
    public void checkPair() throws Exception {
        int[] tileArray = test.checkPair();

        System.out.print(tileArray[0] + " " + tileArray[1] + " " + tileArray[2]);

        Assert.assertEquals(201, tileArray[0]);
        Assert.assertEquals(301, tileArray[1]);
        Assert.assertEquals(302, tileArray[2]);
    }
}

*/

